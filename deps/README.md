# deps (dependencies)

Place to clone external repositories for reference and comparison with RLHF Book.

- Short for **dependencies**: external code this project references.
- Example: clone `nano-aha-moment` here to reference the RL for LLM implementation.

```bash
cd deps
git clone https://github.com/McGill-NLP/nano-aha-moment.git
```
